# quotes-api

## Usage
```
quote [command] [args]

COMMANDS:
add {user} "{quote}" // Add a quote to the user 
list                 // List all users
{user}               // List all quotes of a user
all                  // List all quotes in format {user}: {quote}
```
